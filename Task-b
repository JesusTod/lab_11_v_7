# 7 Варіант. б) Дан файл f, компоненти якого є цілими числами. Знайти кількість парних та
# кількість двозначних чисел серед компонентів файлу. Результат вивести на екран.
# Медуниця Андрій Олексійович | 122г
import random

paired_numbers = 0
double_digit = 0


line = int(input('Введіть кількість чисел: '))

with open('file_f.txt', 'w') as f_0:        # відкрили файл f для писання
    for i in range(line):                     # 10 чисел від 0 до 33
        f_0.write(str(random.randint(0, 33))+'\n')  # в кінці добавили '\n' для того, щоб елементи були на різних рядках


with open('file_f.txt', 'r') as f:          # відкрили файл для читання

    for lines in f:                          # проходимося по числам які знаходяться у файлі

        if int(lines) % 2 == 0:              # якщо число кратне двом,
            paired_numbers += 1             # тоді в paried_numbers додається одиниця

    with open('file_f.txt', 'r') as f_2:    # знову відкрили файл, для пошуку двоцифрових чисел

        for lines in f_2:

            if len(lines) == 3:              # якщо довжина числа == трьом ('\n' теж рахується як елемент, тому 3)
                double_digit += 1           # тоді в double_digit додається одиниця


print('Парних чисел:', paired_numbers)      # виводимо змінні на екран
print('Двоцифрових чисел:', double_digit)
 
